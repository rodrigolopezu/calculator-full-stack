server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: any unknown path falls back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Same-origin proxy to the Go service. The browser only ever talks to this
    # nginx, so no CORS is involved, exactly like the Vite proxy in development.
    # "backend" is the service name in docker-compose.yml.
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}